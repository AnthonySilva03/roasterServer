# Installed to /etc/NetworkManager/conf.d/99-roaster-unmanaged.conf
# Hand wlan0 to hostapd by telling NetworkManager to ignore it. eth0 stays
# managed by NM (useful for wired admin access while wlan0 is the AP).
[keyfile]
unmanaged-devices=interface-name:wlan0
